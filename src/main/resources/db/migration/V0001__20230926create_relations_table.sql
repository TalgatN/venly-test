CREATE TABLE RELATIONS(
                                 ID INTEGER NOT NULL,
                                 word1 VARCHAR(255),
                                 word2 VARCHAR(255) NOT NULL,
                                 relation VARCHAR(255),
                                 PRIMARY KEY (ID)
);